import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const hangUpRed = Colors.redAccent;
  static final darkItemBgColor = Colors.grey.shade800.withOpacity(0.5);
  static const darkForeground = Color(0xFFECEDEE);
  static const darkBackground = Color(0xFF151718);
}
